# Nginx con Docker Compose

Este proyecto configura un servidor web Nginx usando Docker Compose.

## Contenido
- Carpeta src con archivo index.html
- Archivo docker-compose.yml

## Ejecución
docker compose up -d

Luego abrir:
http://localhost:8080
